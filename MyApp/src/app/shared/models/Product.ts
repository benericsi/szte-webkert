export interface Product { 
    id: string;
    title: string;
    img: string;
    img_url: string;
    size: string;
    price: number;
    color: Array<string>;
    pattern: string;
    volume: number;
    from: string;
    quality: string;
}